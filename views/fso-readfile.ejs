<h1>Read File</h1>

<p>Reading file <%= file %></p>
<br/>

<%
  const lines = data.split('\n');
  lines.forEach(function(line) {
    if (line.trim()) {
      const parts = line.split(',');
      if (parts.length >= 2) {
%>
        <p><%= parts[0] %>: <%= parts[1] %></p>
<%
      }
    }
  });
%> 